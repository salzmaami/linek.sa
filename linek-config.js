window.LINEK_LEADS_CONFIG = {
  // Runtime config is loaded from /api/config when available.
  // Do not put a service_role key in this public file.
  supabaseUrl: '',
  supabaseAnonKey: '',
  configEndpoint: '/api/config',

  // Option 2: custom endpoint such as /api/leads.
  // If set, this endpoint is used before Supabase.
  endpoint: ''
};
